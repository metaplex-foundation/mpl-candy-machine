/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  Serializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  getAccountMetasAndSigners,
  ResolvedAccount,
  ResolvedAccountsWithIndices,
} from '../shared';

// Accounts.
export type SetGumballMachineAuthorityInstructionAccounts = {
  /** Gumball Machine account. */
  gumballMachine: PublicKey | Pda;
  /** Autority of the gumball machine. */
  authority?: Signer;
};

// Data.
export type SetGumballMachineAuthorityInstructionData = {
  discriminator: Array<number>;
  newAuthority: PublicKey;
};

export type SetGumballMachineAuthorityInstructionDataArgs = {
  newAuthority: PublicKey;
};

export function getSetGumballMachineAuthorityInstructionDataSerializer(): Serializer<
  SetGumballMachineAuthorityInstructionDataArgs,
  SetGumballMachineAuthorityInstructionData
> {
  return mapSerializer<
    SetGumballMachineAuthorityInstructionDataArgs,
    any,
    SetGumballMachineAuthorityInstructionData
  >(
    struct<SetGumballMachineAuthorityInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['newAuthority', publicKeySerializer()],
      ],
      { description: 'SetGumballMachineAuthorityInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [133, 250, 37, 21, 110, 163, 26, 121],
    })
  ) as Serializer<
    SetGumballMachineAuthorityInstructionDataArgs,
    SetGumballMachineAuthorityInstructionData
  >;
}

// Args.
export type SetGumballMachineAuthorityInstructionArgs =
  SetGumballMachineAuthorityInstructionDataArgs;

// Instruction.
export function setGumballMachineAuthority(
  context: Pick<Context, 'identity' | 'programs'>,
  input: SetGumballMachineAuthorityInstructionAccounts &
    SetGumballMachineAuthorityInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mallowGumball',
    'MGUMqztv7MHgoHBYWbvMyL3E3NJ4UHfTwgLJUQAbKGa'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    gumballMachine: {
      index: 0,
      isWritable: true,
      value: input.gumballMachine ?? null,
    },
    authority: { index: 1, isWritable: false, value: input.authority ?? null },
  };

  // Arguments.
  const resolvedArgs: SetGumballMachineAuthorityInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getSetGumballMachineAuthorityInstructionDataSerializer().serialize(
      resolvedArgs as SetGumballMachineAuthorityInstructionDataArgs
    );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
